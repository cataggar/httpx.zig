name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Format check
        run: zig fmt --check src/ examples/ bench/ build.zig

  test:
    name: Test (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux
          - os: macos-latest
            name: macOS
          - os: windows-latest
            name: Windows
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Cache Zig build
        uses: actions/cache@v6
        with:
          path: .zig-cache
          key: zig-${{ runner.os }}-${{ hashFiles('src/**', 'build.zig', 'build.zig.zon') }}
          restore-keys: zig-${{ runner.os }}-

      - name: Unit tests
        shell: bash
        run: zig build test --summary all --test-timeout 120s
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
          ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache

      - name: Run all examples
        shell: bash
        run: zig build run-all-examples
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
          ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache

      - name: Build (Debug)
        run: zig build -Doptimize=Debug
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
          ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache

      - name: Build (ReleaseFast)
        run: zig build -Doptimize=ReleaseFast
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
          ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache

  cross-build:
    name: Cross Build (${{ matrix.target }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        target:
          # Linux 64-bit
          - x86_64-linux
          - aarch64-linux
          - riscv64-linux
          - sparc64-linux
          - powerpc64-linux
          - mips64-linux
          - mips64el-linux
          - s390x-linux

          # Linux 32-bit
          - x86-linux
          - arm-linux-gnueabihf
          - arm-linux-musleabihf
          - mips-linux
          - mipsel-linux
          - powerpc-linux
          - thumb-linux

          # Linux 64-bit (musl)
          - x86_64-linux-musl
          - aarch64-linux-musl

          # macOS
          - x86_64-macos
          - aarch64-macos

          # Windows 64-bit
          - x86_64-windows
          - aarch64-windows

          # Windows 32-bit
          - x86-windows

          # FreeBSD
          - x86_64-freebsd
          - aarch64-freebsd

          # NetBSD
          - x86_64-netbsd

          # WebAssembly
          - wasm32-wasi
          - wasm32-wasip1

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0

      - name: Cache Zig build
        uses: actions/cache@v6
        with:
          path: .zig-cache
          key: zig-cross-${{ matrix.target }}-${{ hashFiles('src/**', 'build.zig', 'build.zig.zon') }}
          restore-keys: zig-cross-${{ matrix.target }}-

      - name: Install cross-compilation deps
        if: contains(matrix.target, 'linux') && !startsWith(matrix.target, 'x86')
        run: |
          sudo apt-get update
          case "${{ matrix.target }}" in
            aarch64-linux*)
              sudo apt-get install -y qemu-user-static gcc-aarch64-linux-gnu
              ;;
            arm-linux*)
              sudo apt-get install -y qemu-user-static gcc-arm-linux-gnueabihf
              ;;
            riscv64-linux*)
              sudo apt-get install -y qemu-user-static gcc-riscv64-linux-gnu
              ;;
            sparc64-linux*)
              sudo apt-get install -y qemu-user-static
              ;;
            powerpc64-linux*)
              sudo apt-get install -y qemu-user-static gcc-powerpc64-linux-gnu
              ;;
            powerpc-linux*)
              sudo apt-get install -y qemu-user-static gcc-powerpc-linux-gnu
              ;;
            mips64-linux*|mips64el-linux*)
              sudo apt-get install -y qemu-user-static gcc-mips64-linux-gnuabi64
              ;;
            mips-linux*|mipsel-linux*)
              sudo apt-get install -y qemu-user-static gcc-mips-linux-gnu
              ;;
            s390x-linux*)
              sudo apt-get install -y qemu-user-static gcc-s390x-linux-gnu
              ;;
            thumb-linux*)
              sudo apt-get install -y qemu-user-static gcc-arm-linux-gnueabihf
              ;;
          esac

      - name: Build
        run: zig build -Dtarget=${{ matrix.target }} -Doptimize=Debug
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
          ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache
